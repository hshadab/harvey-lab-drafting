# Firm drafting standard for the red flag tracker

<!-- Rule 2 is enforced VERBATIM. Unlike Rule 1, which is a deliberately
     stricter mechanical stand-in for a semantic criterion, LAB's C-043
     states its own list of columns and its own threshold ("FAIL if more
     than two of these columns are missing"), so the check IS the
     criterion. See hook/tracker.py for the measurement against 28 graded
     trackers. Same wording discipline as Rule 1: actor, verb, object,
     standalone number. -->
Rule 2: The agent may not write a final deliverable red flag tracker when
the number of required columns missing from the tracker is more than two.
Writing a final deliverable red flag tracker while the number of required
columns missing from it is more than two is forbidden.
